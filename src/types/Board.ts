export interface Board {
    title: string,
    description: string,
    permissions: string[],
    boardId: string,
    boardColor: string,
    boardTextColor: string,
    editPermission: string[],
}